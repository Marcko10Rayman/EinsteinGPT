public with sharing class MX_BOT_UnifiedKnowledgeURL {

    public class InputValues {
        @InvocableVariable(required= true)
        Public Knowledge__kav obj;
    }

    @InvocableMethod(label='MX_BOT_RertieveURL' description='If the article is an unified knowledge article, retrieve only the url')
    public static void getKnowledge(List<inputValues> input) {
        
    }
}